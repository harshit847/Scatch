<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bags App</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <link rel="icon" href="/icon.png" type="image/png" />
    <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet">

    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-[Roboto]">
    <nav class="w-full flex justify-between px-5 py-3">
        <h3 class="text-xl cursor-pointer">Bags App</h3>

        <% var loggedin=typeof loggedin !=="undefined" ? loggedin : true %>
            <% if(loggedin) { %>
                <form action="/shop" method="GET" class="mb-4">
                    <input type="text" name="search" placeholder="Search products..."
                        value="<%= typeof search !== 'undefined' ? search : '' %>" class="border rounded px-2 py-1" />
                    <button type="submit" class="bg-blue-500 text-white px-4 py-1 rounded ml-2">
                        Search
                    </button>
                </form>
                <div class="flex gap-5">
                    <a href="/shop">Shop</a>
                    <a href="/cart">Cart</a>
                    <a href="/account">My Account</a>
                    <a class="text-red-600" href="/users/logout">Logout</a>
                </div>


                <% } %>
    </nav>